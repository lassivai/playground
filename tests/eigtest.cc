#include <iostream>
#include <eigen3/Eigen/Dense>


/*void rotate(Eigen::Matrix33) {

}*/

int main()
{
  /*
  Eigen::MatrixXd m(3, 3);
  m << 1, 2, 3, 4, 5, 6, 7, 8, 9;

  std::cout << m << "\n\n";
  std::cout << (m*2) << "\n\n";
  std::cout << (m*m) << "\n\n";

  Eigen::MatrixXd a = Eigen::MatrixXd::Random(3, 3);
  Eigen::MatrixXd b = Eigen::MatrixXd::Constant(3, 3, 12);
  Eigen::VectorXd v(3);
  v << 4, 6, -2;

  std::cout << "a*b*v =\n" << (a*b*v) << "\n";

  Eigen::Vector3d p;
*/

  Eigen::Matrix3d;


  return 0;
}
